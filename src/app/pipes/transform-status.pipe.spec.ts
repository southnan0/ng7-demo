import { TransformStatusPipe } from './transform-status.pipe';

describe('TransformStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
