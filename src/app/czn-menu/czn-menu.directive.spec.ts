import { CznMenuDirective } from './czn-menu.directive';

describe('CznMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CznMenuDirective();
    expect(directive).toBeTruthy();
  });
});
